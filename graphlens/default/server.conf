#
# Ensures GraphLens's one custom configuration file (graphlens_settings.conf)
# replicates across search head cluster members, same as every standard
# .conf file already does by default. Only custom conf files need this
# explicit declaration.
#

[shclustering]
conf_replication_include.graphlens_settings = true
